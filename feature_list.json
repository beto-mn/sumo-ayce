{
  "project": "sumo-ayce",
  "description": "SUMO All You Can Eat — website redesign with headless architecture (Nuxt 4 + WordPress) + reservations, loyalty, branch finder and staff portal.",
  "rules": {
    "one_feature_at_a_time": true,
    "require_tests_to_close": true,
    "require_approved_spec_to_implement": true,
    "valid_status": ["pending", "spec_ready", "in_progress", "done", "blocked"],
    "sdd_required_when": "feature has \"sdd\": true"
  },
  "features": [
    {
      "id": 1,
      "name": "db-schema-drizzle",
      "title": "Database Schema & Local Development Environment",
      "description": "Base schema with Drizzle ORM pointing to Neon PostgreSQL in production and Docker Compose locally.",
      "spec_path": "specs/001-db-schema-drizzle",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 2,
      "name": "reservaciones-crud",
      "title": "Reservations — Backend CRUD API",
      "description": "Reservation endpoints with validation, folio generation and Postgres persistence.",
      "spec_path": "specs/002-reservaciones-crud",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 3,
      "name": "twilio-notifications",
      "title": "Reservation Confirmation Flow via WhatsApp",
      "description": "Integration with Twilio WhatsApp Business: customer confirmation, manager notification, escalation via cron.",
      "spec_path": "specs/003-twilio-notifications",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 4,
      "name": "branch-finder-location",
      "title": "Branch Finder by Location",
      "description": "Branch search via geolocation with postal code fallback.",
      "spec_path": "specs/004-branch-finder-location",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 5,
      "name": "loyalty-program",
      "title": "Loyalty Program",
      "description": "Points-based loyalty program with atomic redemption and balance query via WhatsApp.",
      "spec_path": "specs/005-loyalty-program",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 6,
      "name": "staff-portal",
      "title": "Staff Portal",
      "description": "Staff portal with role-based login, dashboard, loyalty transaction management and daily reports.",
      "spec_path": "specs/006-staff-portal",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 7,
      "name": "scaffold-and-design-system",
      "title": "Scaffold & Design System (Mercado Pop)",
      "description": "Verify and install missing libs (Tailwind CSS, @nuxtjs/i18n, mapbox-gl); configure routeRules in nuxt.config.ts per docs/business/rendering-strategy.md; set up design tokens from docs/business/overview.md §2; build base reusable components (Button, Card, Chip, Sticker, Kicker, Input/Select/Textarea, Nav, Marquee) under app/components/ui/; configure default layout; set up i18n with ES default + EN toggle; ensure Storybook coverage for every base component.",
      "spec_path": "specs/007-scaffold-and-design-system",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 8,
      "name": "frontend-test-setup",
      "title": "Frontend Unit Test Setup (Vitest + happy-dom + Vue Test Utils)",
      "description": "Establish frontend testing before any page work begins. Current vitest.config.ts only globs tests/** and server/** — every test under app/ is silently ignored (today there are 2 dead tests in app/composables/ that never run). Scope: (1) update vitest.config.ts include to add app/**/*.test.ts and app/**/*.spec.ts; (2) configure environmentMatchGlobs so app/** runs under happy-dom and everything else stays under node; (3) install happy-dom and @vue/test-utils as devDependencies; (4) audit and re-enable the existing app/composables/useStaffAuth.test.ts and useStaffCustomer.test.ts (currently dead code); (5) establish the convention 'Component.vue ↔ Component.spec.ts co-located' and document it in docs/harness/verification.md and docs/harness/conventions.md; (6) backfill at least one Vitest spec per base component delivered in feature 007 so the pattern is concrete before pages start; (7) extend CHECKPOINTS.md C4 to require pnpm test coverage of app/ (not only server/); (8) tech-debt cleanup carried over from feature 007 reviewer ruling: migrate the 21 inline hex literals in app/components/staff/*.vue and app/pages/staff/*.vue onto the design tokens shipped in feature 007 (`var(--token)` for raw CSS, `bg-orange`/`bg-blue`/`text-ink`/etc. for Tailwind classes). After migration, the T108c grep (`grep -rEn '(style=|: ?)#[0-9a-fA-F]{3,8}\\b' app/components/ app/layouts/ app/pages/`) MUST return zero matches across the entire app/ tree. MUST land after feature 007 (so base components exist to test) and BEFORE feature 009 (homepage) so every page from 009 onward ships with both Storybook stories and Vitest specs.",
      "spec_path": "specs/008-frontend-test-setup",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 9,
      "name": "ci-cd-github-actions",
      "title": "CI/CD via GitHub Actions + Vercel CLI (develop staging, master production)",
      "description": "Stand up CI/CD entirely from GitHub Actions (NOT the native Vercel↔GitHub integration) so the repository never has to grant Vercel direct access. Three workflows under .github/workflows/: (1) ci.yml — runs on every pull request: pnpm install with frozen lockfile, pnpm check, pnpm typecheck, pnpm test (must show ≥226 tests), pnpm build. Cache pnpm-store between runs. Node version pinned to 20 (Vercel runtime). Concurrency group per ref with cancel-in-progress. (2) preview.yml — runs on push to `develop`: pulls Vercel preview env, runs `vercel build`, `vercel deploy --prebuilt`, posts the preview URL as a check output / GitHub Deployments entry. (3) production.yml — runs on push to `master`: pulls Vercel production env, runs `vercel build --prod`, `vercel deploy --prebuilt --prod`. All three consume the three required GitHub secrets: VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID (document the exact dashboard paths to retrieve each). Document required secrets + branch-protection recommendations in docs/harness/ci-cd.md (new). MUST land BEFORE feature 010 (homepage) so every page PR from 010 onward ships with a working preview link. Excluded from scope: native Vercel↔GitHub integration (intentional — see context), DB migration runners, CDN-cache invalidation hooks (these may come later).",
      "spec_path": "specs/009-ci-cd-github-actions",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 10,
      "name": "homepage",
      "title": "Homepage (/)",
      "description": "Public homepage with hero (ALL YOU CAN EAT headline, kicker, price sticker, hero photo), type selector (AYCE / Express cards routing to /menu), featured dishes rail, top 3 promotions, Google reviews, branches CTA band. Content sourcing: top 3 promotions from the WordPress promociones endpoint (server-side); featured dishes and Google reviews from static/hardcoded fixtures committed in the repo (swappable for a real data source later). Rendering mode per docs/business/rendering-strategy.md. Reduced-motion disables marquee/bounce. See docs/business/features.md §1.",
      "spec_path": "specs/010-homepage",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 11,
      "name": "menu-page",
      "title": "Menu / Bebidas Page (/menu)",
      "description": "Menu browsing by location type (AYCE / Express) and modality (All You Can Eat / À la carte — AYCE only) with category chips (scroll navigation) and dish grid. Sauce picker for Wings & Boneless. Per-dish price in à la carte, 'incluido' in All You Can Eat. Content from Neon DB via GET /api/v1/menu. ISR 3600s.",
      "spec_path": "specs/011-menu-page",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 12,
      "name": "promotions-page",
      "title": "Promotions Page (/promotions)",
      "description": "Grid of active promotions from WordPress CPT promociones. Uses the SAME endpoint as the homepage — `GET {WORDPRESS_API_URL}/wp-json/wp/v2/promociones` — with `?activa=1` to fetch ALL active promotions (no `home` filter, no 3-cap). The homepage instead uses `?activa=1&home=1` (home-flagged) with a fallback to `?activa=1`, capped at 3. Query params (activa/home) documented in docs/business/wordpress-endpoints.md. Endpoint + raw shape documented in docs/business/wordpress-endpoints.md; raw response typed in types/wordpress.ts; parsed via server/api/v1/content/validators.ts. Bilingual fields (badge, title, desc, validity). Per-type color coding (Express promos use blue). Inactive promos (acf.activa === false) hidden. The card shows text only; `acf.imagen` is the full promo flyer, opened large in the shared lightbox (app/components/ui/Lightbox.vue) on click — NOT shown inline. Editable in WP admin without code changes. Rendering mode per docs/business/rendering-strategy.md (short ISR — most dynamic content). See docs/business/features.md §3.",
      "spec_path": "specs/012-promotions-page",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 13,
      "name": "branches-page",
      "title": "Branches Page (/sucursales)",
      "description": "Branch finder UI on top of existing backend (feature 004). Branch list cached via ISR per docs/business/rendering-strategy.md; geolocation, postal-code fallback, distance sort and Mapbox map all run client-side over the cached list. Pin colors: orange=AYCE, blue=Express. Card actions: Reserve, Directions, Call. See docs/business/features.md §4.",
      "spec_path": "specs/013-branches-page",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 14,
      "name": "reservation-page",
      "title": "Reservation Page (/reserve)",
      "description": "Dedicated reservation page at /reserve. Branch and type (AYCE/Express) can be pre-filled via query params (?branch=arboledas&type=ayce) so the 'Reservar' button on the branches page routes with context. Form: branch (pre-filled if param present) → branch-dependent time slots from hours, date, party size, name, phone. Submit triggers POST /api/v1/reservations which sends Twilio WhatsApp to client + manager. See docs/business/features.md §5.",
      "spec_path": "specs/014-reservation-page",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 15,
      "name": "loyalty-portal",
      "title": "Loyalty User Portal (/lealtad)",
      "description": "User-facing loyalty portal UI on top of existing backend (feature 005). Registration, login, points balance, transaction history, reward redemption. Rendered SSR (per-user, never cached) per docs/business/rendering-strategy.md. WhatsApp notifications on point/reward events already wired via Twilio. See docs/business/features.md §6.",
      "spec_path": "specs/015-loyalty-portal",
      "sdd": true,
      "status": "pending"
    },
    {
      "id": 17,
      "name": "contact-page",
      "title": "Contact Page (/contact)",
      "description": "Dedicated contact page at /contact. Left panel: form with Nombre, WhatsApp, Elige sucursal (dropdown populated from /api/v1/branches), and Mensaje fields. Submit opens a wa.me deep-link to the selected branch's WhatsApp number with the message pre-composed (no backend, no form submission). Right panel: static contact info (global WhatsApp, email, social network links). Bilingual (ES/EN). SSG rendering (content is static). Branch list fetched client-side so the dropdown is always up to date.",
      "spec_path": "specs/017-contact-page",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 16,
      "name": "menu-schema-db",
      "title": "Menu Database Schema & Migration",
      "description": "Add menu tables (menuCategories, dishes, sauces) + enums (menuLocationType, menuCategoryKey, drinkGroup) to the Drizzle/Neon schema. Each table includes a file_name column (text nullable) for resolving local project image assets. Includes Drizzle migration applied to the production Neon PostgreSQL DB (no Docker), seed data for all 13 menu categories + initial dishes, and query helpers getFeaturedDishes and getFullMenu. A reviewer-approved schema draft already exists in git stash@{0} (branch chore/011-menu-schema-drizzle) — the spec must reconcile it with the file_name addition and confirm the final shape before implementation.",
      "spec_path": "specs/016-menu-schema-db",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 18,
      "name": "vercel-blob-images",
      "title": "Vercel Blob: Menu Image Storage",
      "description": "Migrate menu image delivery from static public/ assets to Vercel Blob Storage. The file_name column in menu_items, menu_categories, and sauces tables already exists and currently stores bare filenames (e.g. wings.jpg). Change its semantic to store the full Blob-relative path (e.g. menu/ayce/wings.jpg — no base URL). Add a BLOB_BASE_URL runtime env var. Update resolveImageUrl() in server/api/v1/menu/index.get.ts to prefix BLOB_BASE_URL instead of building /public/ local paths. Update all seed files (server/db/seeds/ayceMenu.ts, alaCarta.ts, expressMenu.ts, kidsMenu.ts, desserts.ts, drinks.ts, sauces.ts, menuCategories.ts) so file_name values carry the full Blob path. No DB migration needed — column shape is already text nullable. No admin upload UI — the client uploads images manually through Vercel Dashboard or CLI and updates the file_name column directly in the DB. Document upload conventions and the BLOB_BASE_URL env var in docs/harness/vercel-blob.md (new).",
      "spec_path": "specs/018-vercel-blob-images",
      "sdd": true,
      "status": "in_progress"
    },
    {
      "id": 19,
      "name": "homepage-brand-updates",
      "title": "Homepage Hero: Vertical Logo & AYCE Headline Redesign",
      "description": "Two visual updates to the homepage hero section in app/features/homepage/components/HomeHero.vue: (1) Replace the current /brand/sumo-vertical.svg asset with a new client-provided vertical logo asset (same slot, same rounded/rotated frame). (2) Redesign the 'ALL YOU CAN EAT' h1 headline — either swap the text node for a branded image asset (e.g. /brand/ayce-headline.svg or .png) or apply a new typographic treatment (e.g. outlined/stroke text, layered shadow, gradient fill) while keeping the existing i18n key for the alt/aria-label. Decision between image vs. styled text is a spec-phase design call. Changes confined to HomeHero.vue and public/brand/. No backend, no new routes, no DB changes.",
      "spec_path": "specs/019-homepage-brand-updates",
      "sdd": true,
      "status": "pending"
    },
    {
      "id": 20,
      "name": "storybook-full-coverage",
      "title": "Storybook: Complete UI/UX Documentation",
      "description": "Upgrade Storybook from minimal single-story coverage to full documentation-grade. All 50+ Vue components already have at least one story file, but three structural gaps exist: (1) Broken image refs — public/menu/ was deleted in feature 018, stories that reference /menu/**/*.webp will show broken images since staticDirs points to ../public; fix by replacing with placeholder images or null. (2) Missing addon coverage — only @storybook/addon-docs is installed; add @storybook/addon-viewport (mobile/tablet/desktop breakpoints) and @storybook/addon-a11y (accessibility audit). (3) Incomplete story variants — most stories have a single 'Default' story; add missing state variants per component: loading skeleton, empty/no-data, error/validation, disabled, both ES and EN locale. Additionally: enable autodocs globally (set autodocs: true in main.ts instead of 'tag') or add tags: ['autodocs'] to all Meta objects; add proper argTypes with description and control config for interactive props; add a ComponentDocs page/index story for each feature slice summarising the slice's components. No app code changes, no backend, no DB, no new routes — Storybook files and config only.",
      "spec_path": "specs/020-storybook-full-coverage",
      "sdd": true,
      "status": "pending"
    }
  ]
}
