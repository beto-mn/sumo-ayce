{
  "project": "sumo-ayce",
  "description": "SUMO All You Can Eat — website redesign with headless architecture (Nuxt 4 + WordPress) + reservations, loyalty, branch finder and staff portal.",
  "rules": {
    "one_feature_at_a_time": true,
    "require_tests_to_close": true,
    "require_approved_spec_to_implement": true,
    "valid_status": [
      "pending",
      "spec_ready",
      "in_progress",
      "reviewing",
      "done",
      "blocked"
    ],
    "reviewing_required_before_done": true,
    "sdd_required_when": "feature has \"sdd\": true"
  },
  "features": [
    {
      "id": 1,
      "name": "db-schema-drizzle",
      "title": "Database Schema & Local Development Environment",
      "description": "Base schema with Drizzle ORM pointing to Neon PostgreSQL in production and Docker Compose locally.",
      "spec_path": "specs/001-db-schema-drizzle",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 2,
      "name": "reservaciones-crud",
      "title": "Reservations — Backend CRUD API",
      "description": "Reservation endpoints with validation, folio generation and Postgres persistence.",
      "spec_path": "specs/002-reservaciones-crud",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 3,
      "name": "twilio-notifications",
      "title": "Reservation Confirmation Flow via WhatsApp",
      "description": "Integration with Twilio WhatsApp Business: customer confirmation, manager notification, escalation via cron.",
      "spec_path": "specs/003-twilio-notifications",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 4,
      "name": "branch-finder-location",
      "title": "Branch Finder by Location",
      "description": "Branch search via geolocation with postal code fallback.",
      "spec_path": "specs/004-branch-finder-location",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 5,
      "name": "loyalty-program",
      "title": "Loyalty Program",
      "description": "Points-based loyalty program with atomic redemption and balance query via WhatsApp.",
      "spec_path": "specs/005-loyalty-program",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 6,
      "name": "staff-portal",
      "title": "Staff Portal",
      "description": "Staff portal with role-based login, dashboard, loyalty transaction management and daily reports.",
      "spec_path": "specs/006-staff-portal",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 7,
      "name": "scaffold-and-design-system",
      "title": "Scaffold & Design System (Mercado Pop)",
      "description": "Verify and install missing libs (Tailwind CSS, @nuxtjs/i18n, mapbox-gl); configure routeRules in nuxt.config.ts per docs/business/rendering-strategy.md; set up design tokens from docs/business/overview.md §2; build base reusable components (Button, Card, Chip, Sticker, Kicker, Input/Select/Textarea, Nav, Marquee) under app/components/ui/; configure default layout; set up i18n with ES default + EN toggle; ensure Storybook coverage for every base component.",
      "spec_path": "specs/007-scaffold-and-design-system",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 8,
      "name": "frontend-test-setup",
      "title": "Frontend Unit Test Setup (Vitest + happy-dom + Vue Test Utils)",
      "description": "Establish frontend testing before any page work begins. Current vitest.config.ts only globs tests/** and server/** — every test under app/ is silently ignored (today there are 2 dead tests in app/composables/ that never run). Scope: (1) update vitest.config.ts include to add app/**/*.test.ts and app/**/*.spec.ts; (2) configure environmentMatchGlobs so app/** runs under happy-dom and everything else stays under node; (3) install happy-dom and @vue/test-utils as devDependencies; (4) audit and re-enable the existing app/composables/useStaffAuth.test.ts and useStaffCustomer.test.ts (currently dead code); (5) establish the convention 'Component.vue ↔ Component.spec.ts co-located' and document it in docs/harness/verification.md and docs/harness/conventions.md; (6) backfill at least one Vitest spec per base component delivered in feature 007 so the pattern is concrete before pages start; (7) extend CHECKPOINTS.md C4 to require pnpm test coverage of app/ (not only server/); (8) tech-debt cleanup carried over from feature 007 reviewer ruling: migrate the 21 inline hex literals in app/components/staff/*.vue and app/pages/staff/*.vue onto the design tokens shipped in feature 007 (`var(--token)` for raw CSS, `bg-orange`/`bg-blue`/`text-ink`/etc. for Tailwind classes). After migration, the T108c grep (`grep -rEn '(style=|: ?)#[0-9a-fA-F]{3,8}\\b' app/components/ app/layouts/ app/pages/`) MUST return zero matches across the entire app/ tree. MUST land after feature 007 (so base components exist to test) and BEFORE feature 009 (homepage) so every page from 009 onward ships with both Storybook stories and Vitest specs.",
      "spec_path": "specs/008-frontend-test-setup",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 9,
      "name": "ci-cd-github-actions",
      "title": "CI/CD via GitHub Actions + Vercel CLI (develop staging, master production)",
      "description": "Stand up CI/CD entirely from GitHub Actions (NOT the native Vercel↔GitHub integration) so the repository never has to grant Vercel direct access. Three workflows under .github/workflows/: (1) ci.yml — runs on every pull request: pnpm install with frozen lockfile, pnpm check, pnpm typecheck, pnpm test (must show ≥226 tests), pnpm build. Cache pnpm-store between runs. Node version pinned to 20 (Vercel runtime). Concurrency group per ref with cancel-in-progress. (2) preview.yml — runs on push to `develop`: pulls Vercel preview env, runs `vercel build`, `vercel deploy --prebuilt`, posts the preview URL as a check output / GitHub Deployments entry. (3) production.yml — runs on push to `master`: pulls Vercel production env, runs `vercel build --prod`, `vercel deploy --prebuilt --prod`. All three consume the three required GitHub secrets: VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID (document the exact dashboard paths to retrieve each). Document required secrets + branch-protection recommendations in docs/harness/ci-cd.md (new). MUST land BEFORE feature 010 (homepage) so every page PR from 010 onward ships with a working preview link. Excluded from scope: native Vercel↔GitHub integration (intentional — see context), DB migration runners, CDN-cache invalidation hooks (these may come later).",
      "spec_path": "specs/009-ci-cd-github-actions",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 10,
      "name": "homepage",
      "title": "Homepage (/)",
      "description": "Public homepage with hero (ALL YOU CAN EAT headline, kicker, price sticker, hero photo), type selector (AYCE / Express cards routing to /menu), featured dishes rail, top 3 promotions, Google reviews, branches CTA band. Content sourcing: top 3 promotions from the WordPress promociones endpoint (server-side); featured dishes and Google reviews from static/hardcoded fixtures committed in the repo (swappable for a real data source later). Rendering mode per docs/business/rendering-strategy.md. Reduced-motion disables marquee/bounce. See docs/business/features.md §1.",
      "spec_path": "specs/010-homepage",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 11,
      "name": "menu-page",
      "title": "Menu / Bebidas Page (/menu)",
      "description": "Menu browsing by location type (AYCE / Express) and modality (All You Can Eat / À la carte — AYCE only) with category chips (scroll navigation) and dish grid. Sauce picker for Wings & Boneless. Per-dish price in à la carte, 'incluido' in All You Can Eat. Content from Neon DB via GET /api/v1/menu. ISR 3600s.",
      "spec_path": "specs/011-menu-page",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 12,
      "name": "promotions-page",
      "title": "Promotions Page (/promotions)",
      "description": "Grid of active promotions from WordPress CPT promociones. Uses the SAME endpoint as the homepage — `GET {WORDPRESS_API_URL}/wp-json/wp/v2/promociones` — with `?activa=1` to fetch ALL active promotions (no `home` filter, no 3-cap). The homepage instead uses `?activa=1&home=1` (home-flagged) with a fallback to `?activa=1`, capped at 3. Query params (activa/home) documented in docs/business/wordpress-endpoints.md. Endpoint + raw shape documented in docs/business/wordpress-endpoints.md; raw response typed in types/wordpress.ts; parsed via server/api/v1/content/validators.ts. Bilingual fields (badge, title, desc, validity). Per-type color coding (Express promos use blue). Inactive promos (acf.activa === false) hidden. The card shows text only; `acf.imagen` is the full promo flyer, opened large in the shared lightbox (app/components/ui/Lightbox.vue) on click — NOT shown inline. Editable in WP admin without code changes. Rendering mode per docs/business/rendering-strategy.md (short ISR — most dynamic content). See docs/business/features.md §3.",
      "spec_path": "specs/012-promotions-page",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 13,
      "name": "branches-page",
      "title": "Branches Page (/sucursales)",
      "description": "Branch finder UI on top of existing backend (feature 004). Branch list cached via ISR per docs/business/rendering-strategy.md; geolocation, postal-code fallback, distance sort and Mapbox map all run client-side over the cached list. Pin colors: orange=AYCE, blue=Express. Card actions: Reserve, Directions, Call. See docs/business/features.md §4.",
      "spec_path": "specs/013-branches-page",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 14,
      "name": "reservation-page",
      "title": "Reservation Page (/reserve)",
      "description": "Dedicated reservation page at /reserve. Branch and type (AYCE/Express) can be pre-filled via query params (?branch=arboledas&type=ayce) so the 'Reservar' button on the branches page routes with context. Form: branch (pre-filled if param present) → branch-dependent time slots from hours, date, party size, name, phone. Submit triggers POST /api/v1/reservations which sends Twilio WhatsApp to client + manager. See docs/business/features.md §5.",
      "spec_path": "specs/014-reservation-page",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 15,
      "name": "loyalty-portal",
      "title": "Loyalty User Portal (/lealtad)",
      "description": "User-facing loyalty portal UI on top of existing backend (feature 005). Registration, login, points balance, transaction history, reward redemption. Rendered SSR (per-user, never cached) per docs/business/rendering-strategy.md. WhatsApp notifications on point/reward events already wired via Twilio. See docs/business/features.md §6.",
      "spec_path": "specs/015-loyalty-portal",
      "sdd": true,
      "status": "pending"
    },
    {
      "id": 17,
      "name": "contact-page",
      "title": "Contact Page (/contact)",
      "description": "Dedicated contact page at /contact. Left panel: form with Nombre, WhatsApp, Elige sucursal (dropdown populated from /api/v1/branches), and Mensaje fields. Submit opens a wa.me deep-link to the selected branch's WhatsApp number with the message pre-composed (no backend, no form submission). Right panel: static contact info (global WhatsApp, email, social network links). Bilingual (ES/EN). SSG rendering (content is static). Branch list fetched client-side so the dropdown is always up to date.",
      "spec_path": "specs/017-contact-page",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 16,
      "name": "menu-schema-db",
      "title": "Menu Database Schema & Migration",
      "description": "Add menu tables (menuCategories, dishes, sauces) + enums (menuLocationType, menuCategoryKey, drinkGroup) to the Drizzle/Neon schema. Each table includes a file_name column (text nullable) for resolving local project image assets. Includes Drizzle migration applied to the production Neon PostgreSQL DB (no Docker), seed data for all 13 menu categories + initial dishes, and query helpers getFeaturedDishes and getFullMenu. A reviewer-approved schema draft already exists in git stash@{0} (branch chore/011-menu-schema-drizzle) — the spec must reconcile it with the file_name addition and confirm the final shape before implementation.",
      "spec_path": "specs/016-menu-schema-db",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 18,
      "name": "vercel-blob-images",
      "title": "Vercel Blob: Menu Image Storage",
      "description": "Migrate menu image delivery from static public/ assets to Vercel Blob Storage. The file_name column in menu_items, menu_categories, and sauces tables already exists and currently stores bare filenames (e.g. wings.jpg). Change its semantic to store the full Blob-relative path (e.g. menu/ayce/wings.jpg — no base URL). Add a BLOB_BASE_URL runtime env var. Update resolveImageUrl() in server/api/v1/menu/index.get.ts to prefix BLOB_BASE_URL instead of building /public/ local paths. Update all seed files (server/db/seeds/ayceMenu.ts, alaCarta.ts, expressMenu.ts, kidsMenu.ts, desserts.ts, drinks.ts, sauces.ts, menuCategories.ts) so file_name values carry the full Blob path. No DB migration needed — column shape is already text nullable. No admin upload UI — the client uploads images manually through Vercel Dashboard or CLI and updates the file_name column directly in the DB. Document upload conventions and the BLOB_BASE_URL env var in docs/harness/vercel-blob.md (new).",
      "spec_path": "specs/018-vercel-blob-images",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 19,
      "name": "homepage-brand-updates",
      "title": "Homepage & Global Brand/Copy Updates",
      "description": "Broad brand + copy refresh across the homepage and shared layout, plus page-title updates. Full details, exact ES strings, EN translations and design decisions live in specs/019-homepage-brand-updates/client-brief.md (source of truth) and copy-audit.md (i18n key map). Scope: (1) HomeHero.vue — replace /brand/sumo-vertical.svg with the client's illustrated sumo.webp (hero frame only; nav/footer keep sumo-horizontal.svg); render the 'ALL YOU CAN EAT' h1 as CSS-styled real text (Variant A 'flat' box treatment: two staggered lines, ink boxes, orange text, no border/shadow) using the self-hosted Anton font (Google Fonts OFL, @font-face — new webfont dependency), keeping the i18n key for aria; new kicker ('Come sin límites · Buffet preparado al instante') and new subtitle. (2) SiteMarquee.vue — replace all home.marquee items. (3) Homepage SEO (index.vue) — dedicated home.seo.title/description keys. (4) HomeTypeSelector.vue — new kicker 'AYCE - EXPRESS', title 'Dos experiencias, la misma garantía Sumo.', and AYCE/Express card copy. (5) HomeFeaturedRail.vue — add heading; label 'Los favoritos de nuestros clientes' / heading 'Garantía Sumo' / subtitle 'Amado y recomendado por nuestros clientes'. (6) HomeBranchesCta.vue — home.branches.title to 'Más de 30 sucursales en CDMX, EDOMEX y Cuernavaca' (31 branches incl. Cuernavaca verified in seed). (7) SiteFooter.vue — new footer.brand.blurb. (8) Site-wide: replace 'Estilo americano-japonés' with 'Buffet preparado al instante' in brand.tagline + footer.brand.tagline. (9) Page titles (H1 + SEO): Sucursales Sumo / Promociones Sumo / Reservas Sumo. (10) Menu drinks label menu.category.drinks to 'Bebidas y coctelería' (i18n-driven, no prod DB migration; align seed for consistency). All strings updated in both es.json and en.json. Update/verify Storybook stories + Vitest specs for every changed component. No new routes, no DB migration.",
      "spec_path": "specs/019-homepage-brand-updates",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 20,
      "name": "storybook-full-coverage",
      "title": "Storybook: Complete UI/UX Documentation",
      "description": "Upgrade Storybook from minimal single-story coverage to full documentation-grade. All 50+ Vue components already have at least one story file, but three structural gaps exist: (1) Broken image refs — public/menu/ was deleted in feature 018, stories that reference /menu/**/*.webp will show broken images since staticDirs points to ../public; fix by replacing with placeholder images or null. (2) Missing addon coverage — only @storybook/addon-docs is installed; add @storybook/addon-viewport (mobile/tablet/desktop breakpoints) and @storybook/addon-a11y (accessibility audit). (3) Incomplete story variants — most stories have a single 'Default' story; add missing state variants per component: loading skeleton, empty/no-data, error/validation, disabled, both ES and EN locale. Additionally: enable autodocs globally (set autodocs: true in main.ts instead of 'tag') or add tags: ['autodocs'] to all Meta objects; add proper argTypes with description and control config for interactive props; add a ComponentDocs page/index story for each feature slice summarising the slice's components. No app code changes, no backend, no DB, no new routes — Storybook files and config only.",
      "spec_path": "specs/020-storybook-full-coverage",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 21,
      "name": "menu-experience-overhaul",
      "title": "Menu Experience Overhaul (data + UI)",
      "description": "3-way primary selector (AYCE | Express | Bebidas y coctelería) with AYCE modality (All You Can Eat | Carta); curated ordered category sets per selection (buffet filters includedInAyce=true, carta false); deep-linking to any subcategory via URL query params (type/modality/category incl. drink groups) with defaults + fallback; Destilados split into its own drink group (beers_spirits renamed to beers), promo note rendered once, Caguamón-first, café image-first ordering; Vaso Sumo 5→1 consolidated card with flavour selector; non_alcoholic folded into sodas; 11 Garantías Sumo featured; sauces removed from Alitas & Boneless; hover-zoom, 'Carta'/'Menu' label, half-width no-image drink cards. Additive migration 0027 (drink_group.display_order). Full detail in specs/021-menu-experience-overhaul/.",
      "spec_path": "specs/021-menu-experience-overhaul",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 22,
      "name": "homepage-hero-promos-contact",
      "title": "Homepage hero font + Promotions carousel + Contact job card (consolidated)",
      "description": "Consolidated (former 022/023/024). PART A: hero 'ALL YOU CAN EAT' font Titan One → self-hosted Graphik Super (public/fonts/graphik-super.woff2, @font-face + .hero-headline + nuxt.config preload). PART B: WordPress promociones ACF migrated to new contract (badge_es/en, color, tipo, activa, imagen_desktop/tablet/movil media IDs; title from post title.rendered; no titulo/descripcion/vigencia/single imagen) — validators.ts + promotions.get.ts rewritten (3 images resolved with desktop fallback), UiPromotionsCarousel (embla-carousel-vue) shared by homepage + /promotions, <picture> by breakpoint, image + badge overlay; old grid/lightbox retired. Plus handleError: ExternalServiceError → WARN + 502 (graceful WP degradation). PART C: static Bolsa de trabajo card in ContactInfo.vue (ES+EN copy, test phone pill, no form). Full detail in specs/022-homepage-hero-promos-contact/.",
      "spec_path": "specs/022-homepage-hero-promos-contact",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 23,
      "name": "menu-chip-db-drift-guard",
      "title": "Menu Category Chips — DB Drift Guard + Loading Skeletons",
      "description": "PART A (done, reviewer-approved): app/features/menu/menu-sets.ts hardcoded the per-view category/drink-group membership (AYCE_BUFFET_SET, AYCE_CARTA_SET, EXPRESS_SET, DRINKS_SET) consumed by useMenuFilters.ts to build MenuShell.vue's chip row, never cross-checked against what menu-queries.ts actually returns from menu_categories/drink_group — a category removed from the DB without updating menu-sets.ts left a dead/blank chip. Fixed via a pure filterAvailableKeys() guard in menu-sets.ts wired into useMenuFilters/MenuShell (drops curated-set keys absent from the fetched FullMenuResult), plus a CI regression test in tests/db/menu-seeds.test.ts asserting every curated-set key exists in the active seed. sauces/drinkSubGroups tables explicitly out of scope (still in active use). PART B (client-requested, in progress, no formal spec — implemented directly per client instruction): the /menu page (app/pages/menu.vue) fetches via useAsyncData keyed by `menu-${apiType}-${modality}`; only data/error are read today, no pending/status, no loading branch — switching type/modality leaves stale chips/cards mounted with no feedback until the new fetch resolves. Add loading skeletons for the category/drink-group filter chips (MenuCategoryChips.vue) and the dish cards (MenuDishGrid.vue → MenuDishCard.vue) shown while pending, respecting prefers-reduced-motion, on this same branch/feature — do not create a separate feature entry for this.",
      "spec_path": "specs/023-menu-chip-db-drift-guard",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 24,
      "name": "menu-image-refresh-express-branding",
      "title": "Kids AYCE Dish Photo Collage + Sitewide Watermark Background + Express Map Logo",
      "description": "Consolidated client asset-driven visual refresh (3 independent parts, all source assets provided by the client at /Users/betonajera/Downloads/wetransfer_seb08252-copia-webp_2026-07-16_0004/ — spec_author/implementer MUST copy these into specs/024-menu-image-refresh-express-branding/assets/source/ early since the Downloads path is ephemeral/user-machine-local, not part of the repo). PART A — Kids AYCE dish photo: the 'All You Can Eat Kids' item (server/db/seeds/kidsMenu.ts, nameEs 'All You Can Eat Kids', $179 kids buffet) needs its menu image replaced with a NEW composite/collage built from 3 client-provided source photos (KIDS BURGER_B.webp, DSC02525 copia.webp [sushi roll], SEB08252 copia.webp [chicken tenders + fries]) — client explicitly wants all 3 combined into ONE collage image (not a single photo pick), representing the variety of dishes included in the Kids AYCE buffet. Deliver the composite webp and upload/replace it via the existing scripts/replace-blob-images.ts (one-off Vercel Blob ops script, dry-run default, keeps existing pathname so DB/menu-queries stay unchanged) — do not build a new upload pipeline. PART B — Sitewide subtle watermark background: apply the client-provided 'Fondo webp.webp' pattern (a repeating black/white pop-art tile reading 'SUMO express ALL YOU CAN EAT / BURGER / SUSHI / RAMEN' etc.) as a low-opacity (~10-15%) background texture across ALL pages (client confirmed sitewide, not scoped to /menu only or to Express-only context) — likely implemented as a new Tailwind backgroundImage token (alongside the existing hero-pop pattern in tailwind.config.ts) applied at the layout level, tiled/repeated, opacity ~10-15%, MUST NOT reduce text/content contrast below existing accessibility baseline and MUST NOT interfere with any existing background (hero-pop, marquee, etc.) — spec_author should flag as [NEEDS CLARIFICATION] exactly how this composites with existing per-page backgrounds if genuinely ambiguous, and confirm the 'express'-branded pattern being sitewide (not Express-only) is intentional per client answer. PART C — Sumo Express branch map logo: the branch finder map (see docs/business/overview.md §8 — branch map already color-codes pins orange=AYCE / blue=Express) needs actual Sumo Express branding on EXPRESS-type branch markers specifically (client confirmed scope is the branch map ONLY, not the global header/footer SiteLogo.vue) using the client-provided Express logo assets: 'Logo .webp' (stacked/vertical lockup, likely for tight marker space) and 'Logo 2.webp' (horizontal lockup, likely for a popup/info-window on marker click) — spec_author should inspect the current branch-finder map component (feature 004-branch-finder-location) to determine exact marker/popup integration point. Out of scope for this feature: no changes to AYCE branch markers, no global logo swap, no changes to menu-sets.ts or any other menu category/chip logic (unrelated to feature 023).",
      "spec_path": "specs/024-menu-image-refresh-express-branding",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 25,
      "name": "menu-page-lighthouse-perf-fix",
      "title": "Menu Page — Lighthouse Performance & Accessibility Fixes",
      "description": "Fix bug (not a new feature): Lighthouse audit run against /menu (local dev, mobile) surfaced real production-affecting defects, independent of dev-server/extension noise in the raw trace. (1) Oversized menu images served far larger than their displayed dimensions — smash_fries.webp is 2.7MB transferred but displayed at 527x278 (source is 4092x2160, ~2.6MB of avoidable transfer); garantia-sumo.webp badge is 265KB transferred at 128x128 (source 2979x2889); chicken_tenders.webp and french_fries.webp are also moderately oversized. Fix: resize/re-encode these Vercel Blob-hosted assets to dimensions proportional to their actual display size (with reasonable retina headroom), reusing the existing scripts/replace-blob-images.ts one-off ops script — do not build a new upload pipeline. (2) Missing `lang` attribute on `<html>` (html-has-lang audit, score 0) — add lang bound to the active i18n locale in the root app/layout. (3) Insufficient color contrast (color-contrast audit, score 0) on two elements: the 'Reservar' nav button (white text on --orange background, measured ratio 2.67, needs 4.5:1) and the active 'AYCE' modality/category chip (white text on --orange, measured ratio 2.84) — adjust token usage (darker text or darker background) to meet WCAG AA on both without regressing the existing design system tokens. Explicitly OUT of scope: server-response-time (2017ms) and the inflated TTI/Speed-Index/unminified-JS numbers, since those are artifacts of testing against the unminified Nuxt dev server with Chrome extensions (ad blocker, bubble_compiled.js, Vue devtools) active in the trace — re-measure against a production build before treating those as real. No relation to feature 024 (which adds NEW images) — this fixes EXISTING already-shipped assets and accessibility defects.",
      "spec_path": "specs/025-menu-page-lighthouse-perf-fix",
      "sdd": true,
      "status": "pending"
    },
    {
      "id": 26,
      "name": "google-reviews-and-branches-ux",
      "title": "Homepage Google Reviews (WordPress-backed, responsive) + Branches Map/Card UX",
      "description": "Consolidated client request (4 parts, delivered together). PART A — Homepage 'Nos aman en Google' reviews: currently sourced from a static/hardcoded fixture (app/features/homepage/data/reviews.ts, per feature 010's original scope). Client wants this migrated to WordPress — a NEW server endpoint/ACF contract must be designed and built from scratch (no existing WP endpoint for this content; follow the established WP integration pattern in server/api/v1/content/promotions.get.ts / docs/business/wordpress-endpoints.md). Each review needs at minimum reviewer name, review text, rating, and a link to the actual Google review — clicking a review card must navigate to that Google comment/review URL. Responsive display count: mobile shows only 3 reviews, tablet 4, desktop shows all 6 (reason: too much scroll on small viewports today) — use the project's existing 520/880 breakpoint convention. If the WordPress-side ACF fields/content don't exist yet either, spec_author must flag this as a coordination dependency, not silently assume WP-side readiness. PART B — Branches page map/card click sync: clicking a Mapbox branch-pin marker on /branches must trigger the exact same behavior as clicking that branch's card (currently only card-click drives map zoom/pan; pin-click has no reciprocal action) — spec_author should inspect the existing card-click handler in app/pages/branches.vue / app/features/branches/components/BranchList.vue / BranchCard.vue and the mapbox adapter/composable, then wire marker click to invoke that same handler (single source of truth, no duplicated selection logic). PART C — Branches page mobile/tablet scroll UX: on mobile/tablet the branch card list currently causes a long full-page vertical scroll (client-provided screenshot shows cards stacking indefinitely below the map, pushing the footer far down) — client wants the card list to live in its OWN internal scrollable container on mobile/tablet, matching desktop's existing scoped-scroll layout (spec_author must confirm desktop's current layout mechanism and adapt it down to smaller breakpoints, not invent a new pattern). PART D — Branches page scroll-to-map on card click: when a card is clicked, in addition to the EXISTING map zoom/pan behavior (client confirms this already works correctly — 'el zoom al dar click a la card, ya lo hace' — do NOT rebuild it), the page should also scroll the viewport so the map is back in view, needed specifically for mobile/tablet where the user may have scrolled deep into the card list. PARTS C and D must be designed together (a contained/scrollable list container from PART C changes what 'scroll to map' means on those breakpoints) — spec_author should treat them as one coherent mobile/tablet interaction, not two independent tweaks.",
      "spec_path": "specs/026-google-reviews-and-branches-ux",
      "sdd": true,
      "status": "pending"
    },
    {
      "id": 27,
      "name": "promo-flip-menu-card-tweaks",
      "title": "Promotions Carousel Flip-to-Terms + Garantía Star Size + Ramen XL DB-Driven Options + Kids AYCE Background + Vaso Sumo Flavor Migration",
      "description": "Consolidated client-requested visual/UX changes. PART A — Promotions carousel flip-to-terms: clicking anywhere on a promo card in app/components/ui/PromotionsCarousel.vue (used by both HomePromotions.vue on the homepage and app/pages/promotions.vue) must trigger a card-flip animation (3D flip, respecting prefers-reduced-motion with an instant/fade fallback) revealing the promotion's Terms & Conditions on the back face, sourced from WordPress ACF fields `tyc_es`/`tyc_en` on the `promociones` CPT (client is actively wiring these into WP admin now — see specs/027-promo-flip-menu-card-tweaks/assets/source/wp-admin-tyc-fields.png). The flip affordance requires BOTH `tyc_es` AND `tyc_en` to be present and non-empty — a promo with only one language filled in must NOT flip (no same-language fallback). Click-to-flip must not conflict with the carousel's existing drag/swipe (embla-carousel-vue) or the prev/next/dot navigation. PART B — Garantía Sumo star badge size: in app/features/menu/components/MenuDishCard.vue the badge (`size-16`, top-left, shown when dish.featured) is too small to notice — increase to a more prominent size while confirming it still does not overlap the top-right pink badgeEs sticker at any breakpoint. PART C (REVISED 2026-07-16, hero-image approach from the original spec is SCRAPPED) — Ramen XL renders as a NORMAL MenuDishCard (no hero image, no display_variant swap-component), but gains a DB-driven build-your-own options selector matching the client's reference composition (specs/027-promo-flip-menu-card-tweaks/assets/source/ramen-xl-reference.jpg): (1) 'Base de fideo' single-select group — Pollo, Camarón cremoso, Camarón picante, Vegetales picante; (2) 'Proteína' single-select group — Res, Camarón, Pollo; (3) an 'Añade extra proteína' add-on at +$29. ALL of these options (group labels, choices, prices) must be manageable from the DB — client must be able to add/remove/edit noodle-base or protein choices without a code change, analogous to the existing DB-driven `sauces` table/MenuSaucePicker.vue precedent (NOT the Vaso Sumo precedent, which turned out to be i18n-hardcoded, see PART E). Requires new generic DB table(s) for dish option-groups + choices (+ optional price delta per choice) attachable to any menu_item, not just Ramen XL. The abandoned hero-image artifacts from the original spec round (app/features/menu/components/MenuDishHero.vue + .spec.ts + .stories.ts, server/db/migrations/0030_add_menu_item_display_variant.sql, all currently uncommitted) should be removed/superseded as part of this revision unless spec_author finds a reason to keep any part of them. PART D — All You Can Eat Kids card background: the 'All You Can Eat Kids' item card (server/db/seeds/kidsMenu.ts, $179) looks 'sad' against its plain white image panel background — apply an orange→blue gradient behind the image for this one dish only (reusing existing brand tokens and the existing gradient precedent in PromotionsCarousel.vue's navFillClass 'all' case). Needs its own minimal per-dish targeting mechanism (spec_author to decide the simplest correct approach now that PART C no longer needs a shared display_variant column — a dedicated boolean flag or a lightweight reuse, whichever is simpler). PART E (NEW 2026-07-16) — Vaso Sumo flavor migration: the 'Vaso Sumo' drink card (server/db/seeds/drinks.ts, drinkGroup 'cantaritos_sumo_cups') currently renders its 6 base flavors (Ron, Tequila, Vodka, Whisky, New Mix, Jack Daniel's) fully hardcoded via i18n keys `menu.vaso_sumo.flavor.*` in app/features/menu/components/MenuDrinkSection.vue — NOT from the DB. Migrate this onto the SAME generic DB-driven option-groups system built for PART C, so flavors become addable/removable from the DB with no code change, same as the Ramen options. Out of scope: no changes to any other promo card content/layout, no changes to any other menu dish's card size/badge, no changes to Kids items other than the AYCE Kids item's background, no changes to menu-sets.ts curated category logic (unrelated to feature 023), no changes to any drink/dish other than Vaso Sumo and Ramen XL. Full detail in specs/027-promo-flip-menu-card-tweaks/ (spec.md, plan.md, research.md, data-model.md, tasks.md) — MUST be amended to reflect this revision before implementation resumes.",
      "spec_path": "specs/027-promo-flip-menu-card-tweaks",
      "sdd": true,
      "status": "done"
    },
    {
      "id": 28,
      "name": "sauce-thermometer-watermark-refresh",
      "title": "Sauce Heat Thermometer Graphic (Wings/Boneless) + Sitewide Watermark Asset Refresh",
      "description": "Consolidated client asset request (2 independent parts). Source assets provided by the client at /Users/betonajera/Downloads/wetransfer_termometro-salsas-new-webp_2026-07-17_2324/ — already copied by the leader into specs/028-sauce-thermometer-watermark-refresh/assets/source/ (Fondo web bien.webp, Termometro salsas new.webp) since the Downloads path is ephemeral/user-machine-local. PART A — Sitewide watermark asset swap: the current sitewide watermark (feature 024 Part B) is wired as tailwind.config.ts backgroundImage.watermark → url('/patterns/sumo-watermark.webp'), applied via the `bg-watermark` class on the root wrapper in app/layouts/default.vue (`bg-bg bg-watermark bg-repeat`, ~10-15% opacity, see app/layouts/default.spec.ts). Client wants the underlying artwork replaced with the new tile in assets/source/Fondo web bien.webp (same pop-art repeating-tile concept — SUMO / ALL YOU CAN EAT / BURGER / SUSHI / RAMEN / YAKIMESHI / EXPRESS / CRUNCH lockups — just updated artwork). Scope is swapping the asset referenced by /patterns/sumo-watermark.webp (or adding a new token if the new tile's dimensions/aspect ratio don't tile cleanly at the existing settings) — do NOT change the opacity/application mechanism or contrast baseline established in feature 024 unless the new artwork requires it. PART B — Sauce heat thermometer graphic for Wings & Boneless: client wants a visual 'spice thermometer' graphic (reference: assets/source/Termometro salsas new.webp) shown wherever sauce selection happens for the 'Alitas & Boneless' category (server/db/seeds/menuCategories.ts nameEs 'Alitas & Boneless') across AYCE, Express and À la Carta ('salsa a elegir' / '1 aderezo + salsas a elegir' dishes in ayceMenu.ts/expressMenu.ts/alaCarta.ts). IMPORTANT — spec_author must first verify the CURRENT wiring before assuming sauce selection is live: the generic DB-driven option-groups system (menu_item_option_groups/menu_item_option_choices, feature 027 Part C/E) today ONLY has seed rows for Ramen XL and Vaso Sumo (server/db/seeds/menuItemOptions.ts) — no Wings/Boneless dish currently has an option group wired, even though MenuSaucePicker.vue already supports a `sortBySpice` prop and a chili-emoji spice indicator (lines ~52-58, driven by `PickerOption.spiceLevel`) and the `sauces` table (server/db/seeds/sauces.ts) already stores an explicit `spiceLevel` per sauce ordered Parmesano(0) → Jaguar(12) — the exact same low-to-high ordering as the reference thermometer image. spec_author must determine (and may need to newly wire, treating it as in-scope) whether Wings/Boneless dishes need NEW menu_item_option_groups seed rows (sourced from the existing `sauces` table) for the sauce picker to appear at all, then decide where the thermometer graphic mounts relative to it — e.g. once per Alitas & Boneless category section header, or per-dish above/beside each MenuSaucePicker instance — reusing MenuDishCard.vue's existing optionGroups rendering loop (component around line 109) as the integration point, NOT duplicating sauce-selection logic. The client explicitly acknowledged assets/source/Termometro salsas new.webp is a WIP/placeholder from the designer with a large blank left-hand gutter that 'se vera mal' (will look bad) — treat it as a stand-in for layout/integration purposes only; the designer will deliver a corrected image later with (presumably) the same intended layout, so do not hardcode crops/positioning that assume the blank gutter is permanent, and keep the image swap trivial (single asset reference) for that future replacement. Out of scope: no changes to Ramen XL / Vaso Sumo option groups, no changes to any category/menu logic unrelated to Alitas & Boneless, no changes to the sauces table schema (spiceLevel already exists). REVISED 2026-07-17 (client instruction, after receiving the FINAL thermometer graphic from the designer): the original Part B implementation landed at commit df3a13c on feat/028-sauce-thermometer-watermark-refresh — it wired an interactive sauce picker into Wings/Boneless dish cards via NEW menu_item_option_groups/menu_item_option_choices seed rows sourced from the `sauces` table (AYCE/Express single-select, À la Carta bounded multi-select via a new maxSelections column on menu_item_option_groups), plus mounted the section-level thermometer graphic. The client has now decided the thermometer graphic alone is sufficient guidance and the interactive picker + the underlying `sauces` table are NOT wanted — REMOVE: (1) the Wings/Boneless-sourced option-groups seed rows added in df3a13c (server/db/seeds/menuItemOptions.ts) — do NOT touch the pre-existing Ramen XL/Vaso Sumo option groups from feature 027, those stay; (2) the MenuSaucePicker mount for those Wings/Boneless option groups in MenuDishCard.vue's optionGroups loop (Ramen XL/Vaso Sumo pickers must keep working); (3) the `maxSelections` column/prop IF spec_author confirms (via research) it has no other consumer besides the now-removed Wings/Boneless À la Carta multi-select — otherwise leave it as inert/reusable infra per Article I; (4) the entire `sauces` DB table (originally created in feature 016-menu-schema-db, predates this feature) and every remaining reference to it (server/db/schema.ts, server/db/seeds/sauces.ts, server/db/seed.ts, server/utils/menu-queries.ts + .test.ts, types/menu.ts, any dormant `requiresSauce`/`FullMenuResult.sauces` fields noted during the original 028 spec's clarification) — spec_author must verify via fresh grep that nothing outside the menu/wings scope depends on it before finalizing the removal migration (additive-only DB convention still applies — write a new down-leaning migration that DROPs the table rather than editing 0016's original migration file). KEEP unchanged: Part A (watermark artwork swap) and the thermometer graphic itself (already using the client's FINAL asset public/menu/thermometer/sauce-heat-thermometer.webp, already sized full-section-width per client sizing feedback) — both already correctly implemented in df3a13c and must not be reverted. This is a scope reversal of previously-approved FR-007 thru FR-012/User Stories 2-3, not a fresh feature — spec_author must amend the existing spec.md/plan.md/tasks.md in place (mirroring the feature 027 Part C 'SCRAPPED' precedent) and flag anything genuinely ambiguous via [NEEDS CLARIFICATION] rather than guess (e.g. whether maxSelections infra is worth keeping bare for future reuse, or should be dropped too for KISS). PART C (ADDED 2026-07-18, feature was already `done` and reviewer-approved for Parts A/B before this addition — treat as a further in-place amendment, NOT a new feature, since it directly depends on Part B's thermometer graphic which only exists on this feature's own branch): add the copy 'Escoge tu salsa favorita' ('Choose your favorite sauce') to the 'Alitas & Boneless' category, reusing the EXISTING `menu_categories.noteEs`/`noteEn` mechanism (server/db/schema.ts ~lines 263-266, 'Optional section note rendered at the TOP of the category') already live and rendering for the 'kids' category (server/db/seeds/menuCategories.ts ~lines 150-153) via the existing `seedMenuCategories()` upsert — NO new schema, NO new component. Confirmed by prior research on this exact question: `MenuDishGrid.vue` already renders `category.note` directly below the category `<h2>` title and directly above the Part-B thermometer graphic — the note mechanism is live (not dormant), and that existing render order already reads correctly as 'instruction before visual guide', so no layout change is needed. Scope: add `noteEs: 'Escoge tu salsa favorita'` + an EN equivalent (spec_author to pick a natural-sounding EN phrase, e.g. 'Choose your favorite sauce') to the 'Alitas & Boneless' / key 'wings' category entry in server/db/seeds/menuCategories.ts (~lines 118-122), then reseed. Out of scope: no changes to any other category's note, no changes to the thermometer graphic or watermark, no changes to option-groups/sauce-selection logic. PART D (ADDED 2026-07-18, same day as Part C, caught by the client visually inspecting Part C's own output before close-out — fold in as a continuation of Part C, not a separate feature): the `category-note` block (app/features/menu/components/MenuDishGrid.vue ~lines 47-53, class `mb-6 rounded-pop border-pop border-ink bg-yellow px-4 py-3 font-disp font-extrabold text-kicker shadow-pop-sm`) is a block-level `<div>` with no width constraint, so it stretches to the FULL width of its container regardless of text length — this looked fine for the existing 'kids' category's long paragraph note (naturally fills the width), but looks visually wrong for Part C's short 'Escoge tu salsa favorita' note (renders as an oversized pill bar, screenshot confirms). Client wants the note sized to fit its text content, not stretched full-width. Fix: constrain the box to its content width (e.g. Tailwind `w-fit`, which correctly clamps to the container's available width for the long kids-note case — so it still wraps/behaves as today for kids — while hugging short text tightly for the new wings note) — spec_author should verify the exact Tailwind utility/approach against both the short (wings) and long (kids) note content to confirm neither case regresses (kids note must still be fully readable, not clipped or oddly narrow), and should check `MenuDishGrid.spec.ts`/`.stories.ts` for any existing width-related assertions that may need updating. Out of scope: no other visual change to the note (colors, border, shadow, font all stay), no change to note text/content, no change to Parts A/B/C's already-shipped functionality.",
      "spec_path": "specs/028-sauce-thermometer-watermark-refresh",
      "sdd": true,
      "status": "reviewing"
    }
  ]
}
