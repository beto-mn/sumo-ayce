---

description: "Tasks for feature 007 — Scaffold & Design System (Mercado Pop)"
---

# Tasks: Scaffold & Design System (Mercado Pop)

**Input**: Design documents from `/specs/007-scaffold-and-design-system/`
**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md), [data-model.md](./data-model.md), [quickstart.md](./quickstart.md)

**Tests**: This feature ships components WITHOUT Vitest specs — frontend test setup is feature 008's responsibility. Storybook stories are the interim visual review surface (Article VII). See task T200 for the explicit deferral.

**Organization**: Tasks are grouped by user story (from spec.md) so each story can be implemented independently. User Story 1 is the MVP: it requires the shared chassis (deps, tokens, layout, i18n, all 10 base UI primitives) to compose a real page.

## Format: `[ID] [P?] [Story?] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: Which user story this task belongs to (US1, US2, US3, US4, US5)
- Exact file paths included in every task

## Path Conventions

Nuxt 4 monorepo. All paths are repo-relative from `/Users/betonajera/Workspaces/BetoNajera/sumo-ayce/`.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Install the four new libraries and register the corresponding Nuxt modules.

- [x] T001 Add the four new libraries at the EXACT verified pins from `research-versions.md` / research.md §14. Run, in order:
  - `pnpm add -D @nuxtjs/tailwindcss@^6.14.0 tailwindcss@^3.4.19 @nuxtjs/i18n@^10.4.0 @nuxt/fonts@^0.14.0`
  - `pnpm add mapbox-gl@^3.25.0`
  (Tailwind module + Tailwind engine + i18n + fonts as devDependencies; `mapbox-gl` as a regular dependency since it ships at runtime in feature 012.) `tailwindcss ^3.4.19` is pinned explicitly so we don't silently drift onto v4 — the `@nuxtjs/tailwindcss` module pulls v3 transitively, but the explicit pin is the contract. Acceptance: `package.json` shows the four new libraries at exactly these ranges (`@nuxtjs/tailwindcss ^6.14.0`, `tailwindcss ^3.4.19`, `@nuxtjs/i18n ^10.4.0`, `@nuxt/fonts ^0.14.0`, `mapbox-gl ^3.25.0`); `pnpm-lock.yaml` updates; `pnpm install --frozen-lockfile` succeeds; existing deps untouched (Drizzle, Twilio, Storybook 10, Vitest, Biome, `vue-router ^5.0.7` all unchanged).
- [x] T002 Register the new modules in `nuxt.config.ts` by adding `modules: ['@nuxtjs/tailwindcss', '@nuxtjs/i18n', '@nuxt/fonts']`. Do not yet add `routeRules` (T009) or i18n options (T010). Acceptance: `pnpm dev` boots without missing-module errors.
- [x] T003 [P] Configure `@nuxt/fonts` in `nuxt.config.ts` with the chosen families: Bricolage Grotesque (weights `[800]`, provider `'google'`) and Hanken Grotesk (weights `[400, 600, 700]`, provider `'google'`). Acceptance: `pnpm build` resolves both families; no third-party CDN request on first paint of a built page.

**Checkpoint**: All dependencies installed and modules wired. Dev server boots.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Materialize the design tokens, route rules, i18n config, default layout, and the small composable + util that every base component consumes. NOTHING in Phase 3+ can start until this phase is complete.

### Design tokens & global styles

- [x] T004 Create `tailwind.config.ts` at the repo root. **Override `theme.colors` (do NOT use `theme.extend.colors`)** so the Tailwind default palette is NOT generated by the build. The `colors` map MUST be exactly:

  ```ts
  theme: {
    colors: {
      transparent: 'transparent',
      current: 'currentColor',
      bg: 'rgb(var(--bg) / <alpha-value>)',
      bg2: 'rgb(var(--bg2) / <alpha-value>)',
      panel: 'rgb(var(--panel) / <alpha-value>)',
      ink: 'rgb(var(--ink) / <alpha-value>)',
      soft: 'rgb(var(--soft) / <alpha-value>)',
      orange: 'rgb(var(--orange) / <alpha-value>)',
      blue: 'rgb(var(--blue) / <alpha-value>)',
      pink: 'rgb(var(--pink) / <alpha-value>)',
      yellow: 'rgb(var(--yellow) / <alpha-value>)',
      green: 'rgb(var(--green) / <alpha-value>)',
      line: 'rgb(var(--line) / <alpha-value>)',
      accent: 'rgb(var(--accent) / <alpha-value>)',
    },
    extend: { borderRadius: { ... }, boxShadow: { ... }, /* rest */ },
  },
  ```

  Use `theme.extend` for everything else (Mercado Pop tokens from `docs/business/overview.md` §2): `borderRadius` (`pop: 22px`, `pop-sm: 14px`, `pop-full: 9999px`), `boxShadow` (`pop: 6px 6px 0 var(--ink)`, `pop-sm: 4px 4px 0 var(--ink)`), `maxWidth.pop: 1200px`, `fontFamily.disp` and `fontFamily.body` (using the CSS variables), `fontSize` for `h-xl`/`h-lg`/`body`/`kicker`, and `screens: { sm: '520px', md: '880px', lg: '1200px' }`. Set `content: ['./app/**/*.{vue,js,ts}', './.storybook/**/*.{ts,js,vue}']`. **Brand tokens MUST be exposed as peers** — `orange` and `blue` are peer-named (NOT "secondary"), so downstream features 010/012 can switch on Express without re-mapping.

  **Rationale (color format)**: per the official Tailwind v3 docs section [Using CSS variables](https://v3.tailwindcss.com/docs/customizing-colors#using-css-variables), the `rgb(var(--token) / <alpha-value>)` form is required so utility classes with opacity modifiers (`bg-orange/50`, `bg-ink/40`, `hover:bg-accent/90`, etc.) compile correctly. The `<alpha-value>` placeholder is replaced by Tailwind at build time with the modifier value (defaulting to `1` when none is given). This is what shadcn/ui and most production Tailwind v3 design systems do.

  **`theme.colors` is replaced (not extended) so Tailwind's default palette (e.g. `orange-500`, `gray-300`, `red-500`) is NOT generated. This is the primary token-enforcement layer: a typo like `bg-orange-500` produces no compiled class, surfacing the drift visually. `transparent` and `currentColor` are preserved as the only non-token colors. If a future feature needs a new neutral/grey/etc., it MUST be added to `tokens.css` and to this colors map — never re-introduce the default palette.**

  Acceptance:
  - `pnpm typecheck` passes; Tailwind compiles utilities like `bg-bg`, `bg-orange`, `bg-blue`, `bg-accent`, `shadow-pop`, `rounded-pop`, `max-w-pop`, `font-disp`, `text-h-xl`, `md:flex`.
  - Run `pnpm dlx tailwindcss -i tokens.css -o /tmp/test.css --content='./<(echo bg-orange-500 bg-gray-300)>'` (or equivalent) and confirm those classes are NOT present in the output. Run the same with `bg-orange bg-accent shadow-pop` and confirm they ARE present.
- [x] T005 Create `app/assets/css/tokens.css` declaring every token on `:root`. **Color tokens MUST be declared as RGB channels** (space-separated, no commas, no parentheses) so Tailwind opacity modifiers work. Example shape:

  ```css
  :root {
    --bg:     255 247 236;   /* #fff7ec */
    --bg2:    255 233 210;   /* #ffe9d2 */
    --panel:  255 255 255;   /* #ffffff */
    --ink:     26  18   9;   /* #1a1209 */
    --soft:   107  92  73;   /* #6b5c49 */
    --orange: 255 107  43;   /* #ff6b2b */
    --blue:    46 124 246;   /* #2e7cf6 */
    --pink:   255  45 111;   /* #ff2d6f */
    --yellow: 255 194  59;   /* #ffc23b */
    --green:   31 174  90;   /* #1fae5a */
    --line:    26  18   9;   /* same as --ink */
    --accent:  var(--orange);

    --r:        22px;
    --r-sm:     14px;
    /* Shadow tokens MUST wrap --ink in rgb() because --ink is now channels. */
    --shadow:    6px 6px 0 rgb(var(--ink));
    --shadow-sm: 4px 4px 0 rgb(var(--ink));

    --maxw: 1200px;
    --disp: "Bricolage Grotesque", system-ui, sans-serif;
    --body: "Hanken Grotesk", system-ui, sans-serif;
  }
  .scope-express { --accent: var(--blue); }
  ```

  **Confirmed hex values** (resolved 2026-06-17 — prototype values): `--orange: #FF6B2B`, `--blue: #2E7CF6`. Other colors per `docs/business/overview.md` §2. Acceptance: every color token is space-separated RGB channels; file contains exactly ONE `--accent: var(--blue)` declaration; `rgb(var(--bg))` resolves to `#FFF7EC`; `rgb(var(--orange))` resolves to `#FF6B2B`; `rgb(var(--blue))` resolves to `#2E7CF6`; `rgb(var(--accent))` defaults to `#FF6B2B` and switches to `#2E7CF6` inside `.scope-express`; `--shadow` and `--shadow-sm` wrap `var(--ink)` in `rgb(...)`.

  **Rationale (color format)**: per the official Tailwind v3 docs section [Using CSS variables](https://v3.tailwindcss.com/docs/customizing-colors#using-css-variables), color tokens consumed via opacity-modifier utilities must be expressed as RGB channels so the theme entry `rgb(var(--token) / <alpha-value>)` produces a valid color at any alpha. Non-color tokens (radii, shadows, fonts, max-width) stay as-is. Direct CSS consumers of a color token (`background: var(--bg)`) must wrap the call in `rgb(...)` — `background: rgb(var(--bg))` — because the raw value (`255 247 236`) is not a CSS color on its own.
- [x] T006 Create `app/assets/css/base.css` that `@import url('./tokens.css')`, applies a minimal reset (`*, *::before, *::after { box-sizing: border-box; }`), sets `body { background: var(--bg); color: var(--ink); font-family: var(--body); font-size: 17px; line-height: 1.55; }`, and a `.container-pop { max-width: var(--maxw); margin-inline: auto; padding-inline: clamp(16px, 4vw, 32px); }`. Acceptance: file ≤ 60 lines; no token hard-coded.
- [x] T007 Update `nuxt.config.ts` `css` array to include `'~/assets/css/base.css'` AHEAD of the existing `'~/assets/css/staff.css'` so the tokens load first. Acceptance: rendered `<head>` includes `base.css` before `staff.css`.

### Routing & i18n

- [x] T008 Create `i18n.config.ts` at the repo root exporting `defineI18nConfig(() => ({ legacy: false, locale: 'es', fallbackLocale: 'es', missingWarn: true, fallbackWarn: false }))`. Acceptance: file is valid TypeScript and importable.
- [x] T009 Add the canonical `routeRules` block AND `i18n` module options to `nuxt.config.ts`. The `routeRules` block MUST match `docs/business/rendering-strategy.md` §2 EXACTLY: `'/'` → `{ isr: 3600 }`, `'/menu'` → `{ isr: 3600 }`, `'/sucursales'` → `{ isr: 3600 }`, `'/promociones'` → `{ isr: 60 }`, `'/lealtad'` → `{ ssr: true }`, `'/staff/**'` → `{ ssr: true }`, `'/api/**'` → `{}`. The `i18n` block: `{ vueI18n: './i18n.config.ts', locales: [{ code: 'es', name: 'Español', file: 'es.json' }, { code: 'en', name: 'English', file: 'en.json' }], defaultLocale: 'es', strategy: 'prefix_except_default', langDir: 'locales/', detectBrowserLanguage: false }`. Acceptance: `pnpm build` completes with no warnings about route or locale config.
- [x] T010 [P] Create `i18n/locales/es.json` seeded with: `nav.menu`, `nav.promociones`, `nav.sucursales`, `nav.lealtad`, `nav.contacto`, `nav.reservar`, `common.cta.reserve`, `common.cta.viewMenu`, `common.cta.findBranch`, `common.lang.toggle` (= "EN"), `brand.tagline` (= EXACTLY "Estilo americano-japonés"), `brand.ayceBadge` (= EXACTLY "All You Can Eat"). Acceptance: file is valid JSON; `grep -i "buffet" i18n/locales/` returns zero matches.
- [x] T011 [P] Create `i18n/locales/en.json` mirroring every key in `es.json` with English translations: `brand.tagline` = "American-Japanese style", `brand.ayceBadge` = "All You Can Eat", `common.lang.toggle` = "ES". Acceptance: file is valid JSON; `jq -r 'paths(scalars) | join(".")' i18n/locales/en.json | sort` matches the same command run against `es.json` (key sets identical).

### Composables & utilities

- [x] T012 [REMOVED] Composable eliminated — reduced-motion handled via CSS only on Marquee; micro-hover animations are out of scope of the gate per WCAG 2.3.3.
- [x] T013 [P] Create `app/utils/cx.ts` (≤30 LOC) exporting `export function cx(...parts: Array<string | false | null | undefined>): string { return parts.filter(Boolean).join(' ') }`. Acceptance: file ≤ 30 LOC; no library import; pure function.

### Default layout

- [x] T014 Create `app/layouts/default.vue` rendering `<UiNav />` (slot-driven), `<main class="container-pop"><slot /></main>`, and a `<footer>` placeholder with the SUMO brand tagline. Use `<script setup lang="ts">`. Layout MUST NOT import any DB client; MUST NOT import any feature-folder component. Acceptance: file ≤ 100 lines; renders correctly at 360px, 520px, 880px, 1200px.

**Checkpoint**: Foundation ready — base UI components and all user stories can now begin.

---

## Phase 3: User Story 1 — Page-author starts a new public page with the shared chassis (P1) MVP

**Goal**: A developer can compose a page using only base UI components and see the full Mercado Pop look without writing any CSS, with i18n + accent scope working.

**Independent Test**: A throw-away `app/pages/__scaffold-demo.vue` composed exclusively of `<UiButton>`, `<UiCard>`, `<UiChip>`, `<UiSticker>`, `<UiKicker>`, `<UiInput>`, `<UiSelect>`, `<UiTextarea>`, `<UiMarquee>` renders correctly at 360px and 1200px and toggles between ES and EN via the Nav language button.

### Base UI components (parallelizable — different files, no shared edits)

All ten components below share the same shape: `<script setup lang="ts">` with strictly typed `defineProps<>()`, consume Tailwind utilities backed by the tokens from T004, follow the prop tables in [data-model.md §4](./data-model.md). Each task MUST also ship the co-located `*.stories.ts` with: a `Default` story, one story per significant variant (sizes / accent / state), and a mobile (360px) + desktop (1200px) viewport story. `title:` uses `'UI/<ComponentName>'`. `tags: ['autodocs']`.

- [x] T015 [P] [US1] Create `app/components/ui/Button.vue` + `app/components/ui/Button.stories.ts`. Props per data-model.md §4 Button (`variant: 'primary'|'ink'|'ghost'`, `size: 'sm'|'md'|'lg'`, `type`, `disabled`, `loading`). Implements 3px ink border, pill, `shadow-pop-sm`, hover lifts `translate(-2px,-2px)` and grows shadow, active sinks. Acceptance: file < 200 lines; no `any`; renders all 9 combinations in stories (3 variants × 3 sizes); `bg-accent` swaps correctly inside `.scope-express` wrapper story.
- [x] T016 [P] [US1] Create `app/components/ui/Card.vue` + `app/components/ui/Card.stories.ts`. Props per data-model.md §4 Card (`accent`, `tone`, `shadowSize`). When `accent='express'` the rendered root has the `scope-express` class. Acceptance: file < 200 lines; story sweep covers `tone × shadowSize × accent` combinations; AYCE-vs-Express side-by-side story.
- [x] T017 [P] [US1] Create `app/components/ui/Chip.vue` + `app/components/ui/Chip.stories.ts`. Props per data-model.md §4 Chip (`active`, `accent`, `as: 'button'|'span'`). 2.5px border, pill; active fills with `var(--ink)`. Acceptance: file < 200 lines; stories cover `active=false`/`active=true` × `accent=ayce`/`accent=express`.
- [x] T018 [P] [US1] Create `app/components/ui/Sticker.vue` + `app/components/ui/Sticker.stories.ts`. Props per data-model.md §4 Sticker (`tone: 'yellow'|'pink'`, `rotate: number = -8`). Renders as `<span>`, absolutely positioned by parent. Acceptance: file < 100 lines; stories show yellow + pink at -8° and -2°.
- [x] T019 [P] [US1] Create `app/components/ui/Kicker.vue` + `app/components/ui/Kicker.stories.ts`. Props per data-model.md §4 Kicker (`tone: 'ink'|'accent'`, `rotate: number = -2`). Black pill, cream text, slot for label. Acceptance: file < 100 lines; stories cover `tone=ink` and `tone=accent` (AYCE + Express).
- [x] T020 [P] [US1] Create `app/components/ui/Input.vue` + `app/components/ui/Input.stories.ts`. Props per data-model.md §4 Input (`modelValue`, `name`, `type`, `label`, `hint`, `error`, `placeholder`, `required`, `disabled`). 2.5px ink border, 14px radius, focus shows `shadow-pop-sm`. v-model via `update:modelValue`. Acceptance: file < 200 lines; stories cover default, with-label+hint, error state, disabled, all `type` variants.
- [x] T021 [P] [US1] Create `app/components/ui/Select.vue` + `app/components/ui/Select.stories.ts`. Props per data-model.md §4 Select (`modelValue`, `name`, `options: { value, label }[]`, plus shared label/hint/error/required/disabled). v-model via `update:modelValue`. Acceptance: file < 200 lines; stories cover default, with-options, error, disabled.
- [x] T022 [P] [US1] Create `app/components/ui/Textarea.vue` + `app/components/ui/Textarea.stories.ts`. Props per data-model.md §4 Textarea (`rows: number = 4`, plus shared shape). v-model via `update:modelValue`. Acceptance: file < 200 lines; stories cover default, with-label+hint, error, disabled.
- [x] T023 [P] [US1] Create `app/components/ui/Nav.vue` + `app/components/ui/Nav.stories.ts`. Props per data-model.md §4 Nav (`accent`, `sticky`). Sticky top bar, cream bg, 3px bottom ink border. Slots `#logo`, `#links`, `#actions`. Burger overlay at `< md` (880px). The default `#logo` slot fallback uses the existing SUMO logo unmodified (square format, orange background, white "SUMO", black "ALL YOU CAN EAT" bar — pulled from `public/` or `app/assets/` once added; until then, a story-only placeholder with the same shape and a `data-logo="sumo"` attribute). Acceptance: file < 200 lines; stories cover AYCE accent, Express accent, mobile burger collapsed state, desktop full nav.
- [x] T024 [P] [US1] Create `app/components/ui/Marquee.vue` + `app/components/ui/Marquee.stories.ts`. Props per data-model.md §4 Marquee (`speed`, `direction`, `pauseOnHover`). Uses CSS `@keyframes` for horizontal translateX; pauses via `@media (prefers-reduced-motion: reduce) { animation: none }` in its scoped CSS. Acceptance: file < 150 lines; stories cover slow/normal/fast, left/right, and a "reduced-motion" story that forces the media query via Storybook's media-query addon or by toggling the OS setting manually.

### Storybook integration (after components exist)

- [x] T025 [US1] Update `.storybook/main.ts` to extend the `stories` glob with `'../app/components/ui/**/*.stories.@(ts|tsx)'`. Keep any existing globs intact. Acceptance: `pnpm storybook` lists all ten UI components under the "UI" sidebar category.
- [x] T026 [US1] Update `.storybook/preview.ts` to `import '~/assets/css/base.css'` (which transitively imports `tokens.css`) so every story renders with the Mercado Pop look. If using Vite aliases, ensure the path resolves via Storybook's Vite config. Acceptance: stories render with cream background and Bricolage display font; no missing-token visual artifacts.

### MVP integration (manual demo, removed before merge)

- [x] T027 [US1] Create a throw-away page `app/pages/__scaffold-demo.vue` (REMOVED before merge — it is a manual demo) that composes one of each base component to verify the chassis end-to-end. Use locale keys `brand.tagline` and `brand.ayceBadge`. Wrap one section in `<div class="scope-express">` to verify the accent swap. Acceptance: page renders correctly at `pnpm dev`; language button toggles the locale without reload; reduced-motion OS setting pauses the Marquee (verified via the CSS media query, no JS state). **DELETE before merge** — only used for manual MVP validation.

**Checkpoint**: User Story 1 is fully functional. A developer can compose a page using only base components with i18n + accent scope + reduced-motion all wired.

---

## Phase 4: User Story 2 — UI primitive is reviewable in isolation in Storybook (P1)

**Goal**: Every base component is reviewable in Storybook in isolation, with Default + variants + mobile/desktop viewports, no console errors.

**Independent Test**: `pnpm storybook` boots; clicking through every story under "UI/*" produces no runtime errors in the browser console; the SUMO logo (where used) is unmodified.

> NOTE: The per-component story files were already produced as part of Phase 3 (T015–T024). Phase 4 is the verification + polish sweep on top of those stories.

- [x] T028 [US2] Run `pnpm storybook` and walk every story under `UI/*`. For each component, verify: (a) Default renders, (b) every significant variant renders, (c) mobile (360px) and desktop (1200px) viewports render correctly, (d) no console errors. Record any failures and fix them in the originating component task (T015–T024). Acceptance: full sweep produces zero console errors and zero broken visual states.
- [x] T029 [US2] Verify the SUMO logo is used UNMODIFIED everywhere it appears in `Nav.stories.ts` (square format, orange background, white "SUMO", black "ALL YOU CAN EAT" bar). Acceptance: grep / visual inspection confirms no recolor, no crop, no shape change in any story.
- [x] T030 [US2] Run `pnpm storybook:build` to produce a static Storybook bundle. Verify the build completes with no errors. Acceptance: `storybook-static/` directory exists and serves the same sidebar tree as the dev server.

**Checkpoint**: Storybook is the auditable design-system surface. Every component is reviewable.

---

## Phase 5: User Story 3 — Build pipeline honors the rendering strategy (P2)

**Goal**: `pnpm build` picks up the canonical `routeRules` from `nuxt.config.ts`, with no route silently inheriting the default render mode.

**Independent Test**: `pnpm build` succeeds without route-rule warnings, and the seven canonical patterns are present.

- [x] T031 [US3] Diff the `routeRules` block in `nuxt.config.ts` against `docs/business/rendering-strategy.md` §2. The seven entries MUST match exactly (`'/'` → `isr: 3600`, `'/menu'` → `isr: 3600`, `'/sucursales'` → `isr: 3600`, `'/promociones'` → `isr: 60`, `'/lealtad'` → `ssr: true`, `'/staff/**'` → `ssr: true`, `'/api/**'` → `{}`). Acceptance: byte-equivalent (ignoring formatting) match between the doc and the config.
- [x] T032 [US3] Run `pnpm build` and confirm completion with no warnings about unknown or missing route configurations. Acceptance: build exits 0; build log shows ISR entries for `/`, `/menu`, `/sucursales`, `/promociones` and SSR entries for `/lealtad`, `/staff/**`.

**Checkpoint**: Rendering strategy is wired correctly and verified at build time.

---

## Phase 6: User Story 4 — Dependency install is hermetic and reproducible (P2)

**Goal**: From a clean checkout, `pnpm install && pnpm dev` succeeds; the three new libs are at locked versions; no existing dep is removed.

**Independent Test**: A fresh `pnpm install --frozen-lockfile` works; `pnpm dev`, `pnpm test` (server-side suites), `pnpm storybook`, `pnpm check` all succeed.

- [x] T033 [US4] Verify `package.json` lists the four new libraries at the EXACT verified ranges from research.md §14 / `research-versions.md`: `@nuxtjs/tailwindcss ^6.14.0`, `tailwindcss ^3.4.19` (Tailwind engine pinned explicitly so we don't drift onto v4), `@nuxtjs/i18n ^10.4.0`, `mapbox-gl ^3.25.0`, `@nuxt/fonts ^0.14.0` — and NONE of the existing deps (Drizzle, Twilio, Storybook 10, Vitest, Biome, Husky, commitlint, vue-tsc, Pino, `vue-router ^5.0.7`, etc.) were removed or downgraded. Acceptance: `git diff master -- package.json` shows additions only for the four new libs at the listed pins, plus the explicit `tailwindcss` pin; `vue-router ^5.0.7` is unchanged.
- [x] T034 [US4] Run `pnpm install --frozen-lockfile` from a clean state (delete `node_modules`, do NOT delete the lockfile). Acceptance: install completes with no peer-dependency errors that block startup.
- [x] T035 [US4] Run `pnpm dev`, `pnpm test`, `pnpm storybook`, `pnpm check`, `pnpm typecheck` in sequence. Acceptance: all five commands exit 0 (server-side tests stay green; `pnpm storybook` boots; `pnpm check` passes Biome lint + format; `pnpm typecheck` passes vue-tsc).

**Checkpoint**: Reproducibility verified end-to-end.

---

## Phase 7: User Story 5 — Design tokens reachable from Tailwind and raw CSS (P3)

**Goal**: Token values reached through Tailwind utility classes match the values reached through CSS custom properties; the accent scope swap works through both pipes.

**Independent Test**: A Storybook story that uses both `class="bg-bg text-ink"` and `style="background: var(--bg); color: var(--ink)"` renders with pixel-identical colors.

- [x] T036 [US5] Add a "Tokens Smoke" story under `UI/Card.stories.ts` (or a dedicated `UI/_Tokens.stories.ts`) that renders two side-by-side blocks: one using only Tailwind utility classes (`bg-bg text-ink border-ink shadow-pop rounded-pop font-disp text-h-lg`), one using only CSS variables (`style="background: var(--bg); color: var(--ink); border-color: var(--ink); box-shadow: var(--shadow); border-radius: var(--r); font-family: var(--disp); font-size: clamp(32px, 5vw, 60px)"`). Acceptance: visually identical rendering at 1200px.
- [x] T037 [US5] Verify the accent scope swap: wrap one of the blocks above in `<div class="scope-express">` and confirm both pipes resolve `--accent` to `var(--blue)`. Acceptance: `bg-accent` and `background: var(--accent)` both render blue inside the scope, orange outside.

**Checkpoint**: Dual token exposure verified; the design system has a single source of truth.

---

## Phase 8: Polish & Cross-Cutting Concerns (Verification Gates)

**Purpose**: Run the Phase -1 gates from `plan.md` and the explicit success-criteria checks from `spec.md`. These tasks MUST be `[x]` before the feature is marked `done` by the reviewer.

### Article-by-article verification

- [x] T100 Run `grep -rn "drizzle-orm\|@neondatabase/serverless" app/` to confirm ZERO matches under `app/`. (Article V — backend logic never static.) Acceptance: zero matches.
- [x] T101 Run `grep -rn "from 'app/components/staff" app/components/ui/` and `grep -rn "from '@/components/staff" app/components/ui/` to confirm no cross-feature import from `ui/` into `staff/`. (Article I.) Acceptance: zero matches.
- [x] T102 Run a wc-style check on every file under `app/components/ui/` and `app/layouts/`: every `.vue` file MUST be < 200 lines, every `.ts` file MUST be < 200 lines. (Article VIII.) Acceptance: zero files >= 200 lines.
- [x] T103 Run `grep -rn "any" app/components/ui/ app/utils/cx.ts app/layouts/default.vue` and manually confirm no genuine `any` types (allow `any` only inside string literals or comments, none in TypeScript annotations). (Article II.) Acceptance: zero `: any` annotations.
- [x] T104 Run `grep -rn "console.log\|TODO\|FIXME\|XXX" app/components/ui/ app/utils/cx.ts app/layouts/default.vue` to confirm no leftover debug statements / TODO comments. (Article VIII.) Acceptance: zero matches.
- [x] T105 Verify every base component under `app/components/ui/` has a co-located `*.stories.ts` (Article VII). Run `for vue in app/components/ui/*.vue; do test -f "${vue%.vue}.stories.ts" || echo MISSING $vue; done` — output MUST be empty. Acceptance: every `.vue` has its story file.
- [x] T106 Run `grep -rn "\-\-accent: var(\-\-blue)" app/` and confirm exactly ONE match (in `tokens.css`, inside the `.scope-express` declaration). (Article VII — single-class accent swap.) Acceptance: exactly one match.
- [x] T107 Run `grep -rni "buffet" i18n/ app/` to confirm zero matches. (Brand-copy rule from `docs/business/features.md` §0.) Acceptance: zero matches.
- [x] T108 Run `grep -rn "comida japonesa" i18n/ app/` to confirm zero matches (case-sensitive; the locked value is "Estilo americano-japonés"). Acceptance: zero matches.
- [x] **T108a [Token enforcement — default palette]** Run:
```
grep -rEn '(bg|text|border|ring|fill|stroke|divide|outline|shadow|placeholder|accent|caret|decoration)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-(50|100|200|300|400|500|600|700|800|900|950)\b' app/ .storybook/ 2>/dev/null
```
Acceptance: zero matches. Any match means the Tailwind default palette leaked into the build — fix by replacing with a project token (`bg-orange`, `bg-accent`, etc.) and, if no suitable token exists, add it to `tokens.css` and `tailwind.config.ts` first.
- [x] **T108b [Token enforcement — arbitrary values]** Run:
```
grep -rEn '(bg|text|border|ring|fill|stroke|outline|shadow|from|to|via)-\[' app/ .storybook/ 2>/dev/null
```
Acceptance: zero matches. Arbitrary values (`bg-[#FF6B2B]`, `text-[14px]`) bypass the token contract. If you need a one-off, add a real token.
- [x] **T108c [Token enforcement — inline hex / raw colors]** Run:
```
grep -rEn '(style=|: ?)#[0-9a-fA-F]{3,8}\b' app/components/ app/layouts/ app/pages/ 2>/dev/null
```
Acceptance: zero matches outside of `app/assets/css/tokens.css` (where the canonical hex values live). Use `var(--token)` for raw CSS, Tailwind utility classes elsewhere.
- [x] T109 Run `grep -rn "\.\./\.\./" app/components/ui/` and `grep -rn "\.\./\.\./" app/utils/cx.ts` to confirm no relative paths crossing more than one directory boundary. (Article XI.) Acceptance: zero matches.
- [x] T110 Run `pnpm check` (Biome lint + format), `pnpm typecheck` (vue-tsc), and a representative `git commit` to confirm Husky + commitlint accept the Conventional Commit format and reject `--no-verify` paths. (Article IX.) Acceptance: all three gates exit 0; commitlint accepts `feat(007): ...` style messages.

### Build & runtime checks

- [x] T111 Run `pnpm build` end-to-end. Acceptance: build exits 0; no warnings about route rules; output contains the expected ISR + SSR markers for the seven canonical routes.
- [x] T112 Manually toggle the OS `prefers-reduced-motion: reduce` setting and open the Storybook story for `Marquee`. Acceptance: Marquee animation pauses (visually static); Marquee layout intact. Micro-hover animations on Button/Card/Sticker are NOT gated and remain active — that is intentional.

### Documentation & state updates

- [x] T113 Delete the throw-away demo page `app/pages/__scaffold-demo.vue` created in T027 (if not already removed). Acceptance: file absent.
- [x] T114 Update `progress/current.md` recording: feature 007 completed; spec/plan/tasks paths; main Phase -1 gates passed. Note that the three pre-decisions (Q1 brand colors = `#FF6B2B` / `#2E7CF6`, Q2 typography = Bricolage + Hanken, Q3 i18n routing = `prefix_except_default`) were resolved by the human on 2026-06-17 before implementation began. Acceptance: the file references this feature's spec folder and lists the closed gates.

### Deferred work — explicitly handed to feature 008

- [x] T200 Record the deferral of Vitest specs for `app/**` in a single line at the top of `tasks.md` (already present in this file's header) AND ensure feature 008's spec (`specs/008-frontend-test-setup/spec.md`, generated later) references this feature's base-component list (Button, Card, Chip, Sticker, Kicker, Input, Select, Textarea, Nav, Marquee) as the backfill target. **No code change in this feature** — this task only verifies that the handoff is documented. Acceptance: the deferral note is present in this `tasks.md` AND `feature_list.json` id=8 description still lists "(6) backfill at least one Vitest spec per base component delivered in feature 007".

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Setup)**: No upstream — start immediately. Tasks T001–T003 sequentially (T001 → T002 → T003 because they edit `package.json` and `nuxt.config.ts`).
- **Phase 2 (Foundational)**: Depends on Phase 1. Tasks T004 → T005 → T006 → T007 sequential (each edits config or imports the prior). T008 → T009 sequential. T010, T011 parallel after T009. T012, T013 parallel — no shared edits. T014 after T005 (needs tokens).
- **Phase 3 (US1 — Base components)**: Depends on Phase 2 complete. T015–T024 are ALL parallel (`[P]`) — each task owns two new files. T025 → T026 sequential after components exist (both touch `.storybook/`). T027 last (consumes all components).
- **Phase 4 (US2 — Storybook review)**: Depends on Phase 3 (components + stories must exist).
- **Phase 5 (US3 — Build pipeline)**: Depends on Phase 2 T009 (route rules). Independent of US1/US2 components.
- **Phase 6 (US4 — Reproducibility)**: Depends on Phase 1 T001 (deps installed).
- **Phase 7 (US5 — Dual token exposure)**: Depends on Phase 3 components + Storybook (T026).
- **Phase 8 (Polish & gates)**: Depends on all prior phases. T100–T112 sequential is fine (each one is fast); T113 → T114 at the very end. T200 can be done at any time.

### User Story Dependencies

- **US1 (P1, MVP)**: Phase 2 → Phase 3. Self-contained.
- **US2 (P1)**: Builds on US1 — verifies the stories US1 created.
- **US3 (P2)**: Only needs T009. Can run in parallel with US1 once Phase 2 is done.
- **US4 (P2)**: Only needs T001 (dep install). Can run in parallel with everything.
- **US5 (P3)**: Builds on US1 + US2.

### Parallel Opportunities

**Phase 2** parallel set: `{T010, T011, T012, T013}` after T009 lands.

**Phase 3** parallel set: `{T015, T016, T017, T018, T019, T020, T021, T022, T023, T024}` — all ten components can be written simultaneously by parallel implementers. Each task owns its `.vue` + its `.stories.ts`. T025 + T026 follow once at least the first component lands.

**Phases 4–7** can be partially parallelized once Phase 3 is complete (different reviewers, different verification surfaces).

---

## Parallel Example: Phase 3 (US1) — base UI components

```bash
# Ten parallel implementers (or one implementer iterating). Each task touches
# exactly two new files; no shared edits.
Task T015: Create app/components/ui/Button.vue + Button.stories.ts
Task T016: Create app/components/ui/Card.vue + Card.stories.ts
Task T017: Create app/components/ui/Chip.vue + Chip.stories.ts
Task T018: Create app/components/ui/Sticker.vue + Sticker.stories.ts
Task T019: Create app/components/ui/Kicker.vue + Kicker.stories.ts
Task T020: Create app/components/ui/Input.vue + Input.stories.ts
Task T021: Create app/components/ui/Select.vue + Select.stories.ts
Task T022: Create app/components/ui/Textarea.vue + Textarea.stories.ts
Task T023: Create app/components/ui/Nav.vue + Nav.stories.ts
Task T024: Create app/components/ui/Marquee.vue + Marquee.stories.ts
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup (T001–T003).
2. Complete Phase 2: Foundational (T004–T014) — CRITICAL; blocks every component.
3. Complete Phase 3: User Story 1 (T015–T027) — ten parallel components + Storybook integration.
4. **STOP and VALIDATE**: open Storybook + the demo page; verify the Mercado Pop look, language toggle, and accent scope work.
5. Optional: ship MVP behind a feature flag (or just demo internally).

### Incremental Delivery

1. Phase 1 + Phase 2 → Foundation ready.
2. Phase 3 → US1 (MVP) → Demo.
3. Phase 4 → US2 (Storybook review polish).
4. Phase 5 → US3 (build pipeline verification).
5. Phase 6 → US4 (reproducibility verification).
6. Phase 7 → US5 (dual token exposure verification).
7. Phase 8 → all gates pass → feature `done`.

### Parallel Team Strategy

If multiple implementers are available:

1. One pair completes Phase 1 + Phase 2 sequentially.
2. Once Phase 2 is `done`, hand out T015–T024 to ten parallel implementers (or two implementers each owning five).
3. A separate implementer can start US3 (T031–T032) and US4 (T033–T035) in parallel — they don't depend on Phase 3.
4. Phase 8 verification gates run by the reviewer after everything else is `[x]`.

---

## Notes

- [P] = different files, no shared dependency.
- [Story] label maps tasks to spec.md user stories (US1–US5).
- This feature ships NO Vitest specs under `app/**`. Feature 008 (`frontend-test-setup`) will backfill them. See T200.
- The brand-copy locked values ("Estilo americano-japonés", "All You Can Eat") are enforced by T107/T108 grep checks.
- The single-class accent swap (`.scope-express { --accent: var(--blue); }`) is enforced by T106 (exactly-one-match grep).
- The exact match between `nuxt.config.ts` route rules and `docs/business/rendering-strategy.md` §2 is enforced by T031 + T032.
- The SUMO logo is used unmodified everywhere it appears (verified by T029).
- The throw-away demo page from T027 MUST be deleted before merge (T113).
- Pre-decisions Q1/Q2/Q3 from spec.md are RESOLVED (2026-06-17): Q1 brand colors = prototype values (`--orange: #FF6B2B`, `--blue: #2E7CF6`, with Express as a peer brand token — NOT a secondary); Q2 typography = Bricolage Grotesque (800) + Hanken Grotesk via `@nuxt/fonts`; Q3 i18n routing = `prefix_except_default`. These values are baked into T003, T004, T005, T009.
